var strStr = function(haystack, needle) {
    if(haystack === '') return 0;
    if(!(haystack.includes(needle))) return -1;
    let j = haystack.indexOf(needle, needle.length);
    for(let i = 0; i < haystack.length; i++) {
        
    }
    
};